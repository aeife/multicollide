'use strict';

describe('Directive: confirmDialoge', function () {
  beforeEach(module('angularappApp'));

  var element;

  // it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //   element = angular.element('<confirm-dialoge></confirm-dialoge>');
  //   element = $compile(element)($rootScope);
  //   expect(element.text()).toBe('this is the confirmDialoge directive');
  // }));
});
