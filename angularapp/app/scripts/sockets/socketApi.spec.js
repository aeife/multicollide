'use strict';

describe('Service: socketApi', function () {

  // load the service's module
  beforeEach(module('angularappApp'));

  // instantiate service
  var socketApi;
  beforeEach(inject(function (_socketApi_) {
    socketApi = _socketApi_;
  }));

  // it('should do something', function () {
  //   expect(!!socketApi).toBe(true);
  // });

});
